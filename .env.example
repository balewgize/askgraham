# Used by `pnpm classify` and `pnpm paths` (scripts/classify-essays.ts, scripts/generate-paths.ts).
# Any OpenRouter model that supports structured outputs works.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-v4.1-flash
